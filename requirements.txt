# Human-edited floor versions. CI and reproducible installs use the fully
# resolved requirements.lock.txt instead — regenerate it after any change here:
#   pip install pip-tools
#   pip-compile requirements.txt --output-file=requirements.lock.txt --resolver=backtracking

# Core data
# Upper bound pinned deliberately: yfinance chases Yahoo's shifting private
# endpoints with frequent breaking releases, and it relies on curl_cffi for the
# browser impersonation that gets past Yahoo's bot detection. Bump both together
# after testing a new yfinance against a real scan. requirements.lock.txt is the
# reproducible install; this ceiling stops an unpinned `pip install -U` pulling
# a yfinance/curl_cffi combination that silently stops returning data.
yfinance>=0.2.36,<2.0.0
curl_cffi>=0.7.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0
# yfinance's get_earnings_dates() (fundamental_client.get_eps_growth_trend) reads
# Yahoo's earnings-calendar history via pandas.read_html, which needs lxml.
lxml>=5.0.0

# Config / env
pyyaml>=6.0
python-dotenv>=1.0.0

# HTTP / API clients
requests>=2.31.0
urllib3>=2.0.0

# Claude API — V3 deep-analysis synthesis stage (deep_analysis/synthesize.py).
# Imported lazily; only needed to actually generate a briefing.
anthropic>=0.40.0

# Options math (Black-Scholes)
py_vollib>=1.0.1

# spacy/click removed 2026-08-24 — ner_extractor.py's spaCy scaffold
# (load_nlp()) was never actually wired into extract_ticker_sentiments;
# ticker sentiment extraction is, and always was, pure keyword matching.
# See shared/utils/ner_extractor.py's module docstring.

# Scheduling (paper trading / run cadence)
schedule>=1.2.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Linting / static analysis
ruff>=0.6.0

# Utilities
python-dateutil>=2.8.0
pytz>=2023.3

# Desktop app UI (see App_UI_Scope.md)
# Capped below 6.10 — 6.10.0+ has never shipped a Linux wheel (Windows-only
# so far), which breaks `pip install` in CI (ubuntu-latest). Raise the cap
# once a Linux wheel appears for a newer release.
PySide6>=6.7.0,<6.10.0
