# DAY-ONLY: Entry point for backtesting the day model specifically.
# Instantiates the day scorer and trade selector, passes them to BacktestEngine,
# and writes the results report to backtesting/reports/.


def main():
    """Load day config, run backtest engine with day scorer/selector, and output metrics report."""
    pass


if __name__ == "__main__":
    main()
