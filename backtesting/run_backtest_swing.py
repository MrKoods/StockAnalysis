# SWING-ONLY: Entry point for backtesting the swing model specifically.
# Instantiates the swing scorer and trade selector, passes them to BacktestEngine,
# and writes the results report to backtesting/reports/.


def main():
    """Load swing config, run backtest engine with swing scorer/selector, and output metrics report."""
    pass


if __name__ == "__main__":
    main()
